<!-- res/drawable/edit_text_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="8dp"/> <!-- Adjust the radius for rounded corners -->
    <solid android:color="@android:color/white" /> <!-- Background color -->
    <stroke
        android:width="1dp"
        android:color="@color/gray" /> <!-- Border color -->
</shape>
